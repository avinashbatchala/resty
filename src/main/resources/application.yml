spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
welcome:
  message: Hi Avinash
